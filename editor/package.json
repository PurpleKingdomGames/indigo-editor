{
  "scripts": {
    "start": "parcel index.html --no-cache --dist-dir dist --log-level info",
    "build": "parcel build index.html --dist-dir dist --log-level info --public-url ."
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.7.0",
    "parcel": "^2.1.0",
    "parcel-reporter-static-files-copy": "^1.3.4",
    "process": "^0.11.10"
  },
  "staticFiles": {
    "staticPath": "static"
  },
  "dependencies": {
    "bootstrap": "^5.0.1"
  }
}
